package yahtzee.view;

/**
 * Interface for UI components or models that can be reset to initial state.
 */
public interface Resettable {
    /**
     * Reset internal state to defaults.
     */
    void reset();
}